<div class="container">
  <% @article.each do |articlee| %>
    <div class="row justify-content-md-center">
      <div class="col-8 mt-4">
        <div class="card text-center shadow mb-5 bg-white rounded">
          <div class="card-header font-italic">
            <% if articlee.user %>
              by <%= link_to articlee.user.username, user_path(articlee.user) %>
            <% end %>
          </div>
          <div class="card-body">
            <h5 class="card-title"><%= link_to articlee.title, article_path(articlee), class: "text-success" %></h5>
            <p class="card-text"><%= truncate(articlee.description, length: 100) %></p>
            <%= link_to "View", article_path(articlee), class: "btn btn-outline-success" %>
            <%= link_to "Edit", edit_article_path(articlee), class: "btn btn-outline-info" %>
            <%= link_to "Delete", article_path(articlee), class: "btn btn-outline-danger", method: :delete, data: {confirm: "Are you sure?"} %>
          </div>
          <div class="card-footer text-muted">
            <small>Created <%= time_ago_in_words(articlee.created_at) %> ago, 
            edited <%= time_ago_in_words(articlee.updated_at) %> ago</small>
          </div>
        </div>
      </div>
    </div>
  <% end %>